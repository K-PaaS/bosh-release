<%=

postgres_config = {
  "name" => "postgres-13",
  "executable" => "/var/vcap/jobs/postgres-13/bin/postgres",
  "persistent_disk" => true,
  "limits" => {
    "open_files" => 65536,
  },
  "hooks" => {
    "pre_start" => "/var/vcap/jobs/postgres-13/bin/pre-start",
  }
}

config = {
  "processes" => [postgres_config],
}

YAML.dump(config)

%>
